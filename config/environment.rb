# Set up Bundler here following the instructions in the README
# should require bundler/setup in the environment (FAILED - 1)
# should require the default and development groups in the environment (FAILED - 2)

require 'bundler/setup'
Bundler.require(:default, :development)